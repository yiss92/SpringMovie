package controller;

public class free_boardController {
	private free_boardService service;

	public free_boardController(free_boardService service) {
		this.service = service;
	}
	
	setService
}
